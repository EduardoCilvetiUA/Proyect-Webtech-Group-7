<p style="color: green"><%= notice %></p>

<h1>Tickets</h1>

  <table class="table-bordered">
    <thead>
    <tr>
      <th>ID</th>
      <th>Executive ID</th>
      <th>Supervisor ID</th>
      <th>Title</th>
      <th>Description</th>
      <th>Priority</th>
      <th>Due Date</th>
      <th>Status</th>
      <th>Star rating</th>
      <th>Comment</th>
      <th>Created at</th>
      <th></th>
    </tr>
    <tbody>
      <% @tickets.each do |tickets| %>
    <tr>
      <td><%= tickets.id %></td>
      <td><%= tickets.executive_id %></td>
      <td><%= tickets.supervisor_id %></td>
      <td><%= tickets.title %></td>
      <td><%= tickets.description %></td>
      <td><%= tickets.priority %></td>
      <td><%= tickets.due_date %></td>
      <td><%= tickets.status %></td>
      <td><%= tickets.star_rating %></td>
      <td><%= tickets.comment %></td>
      <td><%= tickets.created_at %></td>
      <td><%= link_to "Show ticket info", tickets, class: 'btn btn-outline-warning' %>
    </tr>
    <% end %>
    <tbody>
  </table>
<%= link_to "New tickets", new_ticket_path, class: 'btn btn-outline-warning' %>
